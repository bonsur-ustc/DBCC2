function prob = get_info(func_num)
% get information of problem: ranges, dimension, maxFEs
switch(func_num)
    case 1
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 2
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 3
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 4
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 5
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 6
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 7
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 8
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 9
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 10
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 11
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 12
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 13
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 14
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
    case 15
        prob.lb = -100*ones(1,10);
        prob.ub =  100*ones(1,10);
        prob.dim = 10;
        prob.maxFEs = 1000000;
end
end

